import React, { Component } from "react";

class Contacts extends Component {
  state = {};

  render() {
    const { isOpen } = this.props;

    return <div class="label" style={{ display: "none" }} />;
  }
}

export default Contacts;
